// mui-config.ts
import { Button } from '@mui/material';

export const buttonStyles = {
  primary: {
    variant: 'contained',
    color: 'primary',
  },
  secondary: {
    variant: 'contained',
    color: 'secondary',
  },
};

export const customComponents = {
  Button,
};